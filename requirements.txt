yfinance>=0.2.31
alpaca-py>=0.21.0
pandas>=2.0.0
numpy>=1.24.0
pandas_market_calendars>=4.4.0
scipy>=1.11.0
python-dotenv>=1.0.0
requests>=2.31.0
pytest>=7.4.0
pytest-mock>=3.12.0
streamlit>=1.42.0
plotly>=6.0.0
# watchdog drives event-based dashboard refresh + cache invalidation
# (trading_agent/streamlit/file_watcher.py). Set WATCHDOG_DISABLE=1 to
# fall back to mtime+size-based polling.
watchdog>=3.0,<5

# --- Optional: FinGPT multi-source pipeline ---
# Install the sources you want; each degrades gracefully if absent.
# Reddit (r/wsb, r/stocks, r/options):   pip install praw>=7.7.0
# Twitter / X cashtag stream:             pip install tweepy>=4.14.0
# Claude verifier (VERIFIER_PROVIDER=anthropic): pip install anthropic>=0.40.0
